// This file re-exports all API services from the modularized structure for backward compatibility
// When implementing the actual API, replace the implementations in the individual service files

// Re-export everything from the modular API structure
export * from './api/index';
